services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: nextcrm
      POSTGRES_PASSWORD: nextcrm
      POSTGRES_DB: nextcrm
    dns:
      - 8.8.8.8
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
